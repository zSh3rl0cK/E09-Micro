#include <stdio.h>
#include <avrt.h>

int main(void) {
    // de acordo com o yvo por padrão o char tem os 8bits
    unsigned char contador; // pode ser short int no lugar de char
    signed short int entrada; // pode ser char no lugar de int

    unsigned int tempo;
    signed int temperatura;

    const int IN = 0x16d;

    return 0;
}